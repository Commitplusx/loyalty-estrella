import 'package:flutter/material.dart';

class GhostTrailMap extends StatelessWidget {
  final int repartidorId;

  const GhostTrailMap({
    Key? key,
    required this.repartidorId,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) => const SizedBox();
}
