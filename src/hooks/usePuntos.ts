// usePuntos.ts — Hook descontinuado.
// Las funciones que dependían de este hook (acumularPuntoRPC, canjearEnvioGratisRPC)
// fueron eliminadas de supabase.ts por ser dead code (no se usaban en ningún componente activo).
// Si se necesita un admin panel en el futuro, reimplementar usando fn_registrar_entrega_bulk
// y canjear_saldo directamente.
export {};
